---
interface Props {
  text: String;
  className: String;
  link: URL | "#";
}

const { text, link, className = "" } = Astro.props;
---

<a
  href={link}
  class=`font-medium text-sm px-2 py-1 hover:text-primary duration-300 ${className}`
  >{text}</a
>
